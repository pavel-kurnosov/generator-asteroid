language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-asteroid' ]; then cd .. && eval "mv $currentfolder generator-asteroid" && cd generator-asteroid; fi

